apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: myweb
spec:
  rules:
    - host: gos.my.id
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: myweb
                port:
                  number: 80

